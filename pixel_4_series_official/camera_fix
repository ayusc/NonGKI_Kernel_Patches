diff --git a/fs/proc/fd.c b/fs/proc/fd.c
index e424d5249..443730359 100644
--- a/fs/proc/fd.c
+++ b/fs/proc/fd.c
@@ -84,25 +84,37 @@ static int seq_show(struct seq_file *m, void *v)
 			goto orig_flow;
 		}
 		dpath = d_path(&file->f_path, pathname, PAGE_SIZE);
-		if (IS_ERR(dpath)) {
-			goto out_kfree;
-		}
-		if (kern_path(dpath, 0, &path)) {
-			goto out_kfree;
-		}
-		if (!path.dentry->d_inode) {
-			goto out_path_put;
+		
+		/* Only attempt SusFS hide logic if it's a valid, real file path */
+		if (!IS_ERR(dpath) && kern_path(dpath, 0, &path) == 0) {
+			
+			/* Safe check for valid dentry and inode */
+			if (!path.dentry || !path.dentry->d_inode) {
+				path_put(&path);
+				goto out_kfree;
+			}
+
+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
+			if (susfs_is_sus_path(&path)) {
+				path_put(&path);
+				goto out_kfree; /* Hide the FD */
+			}
+#endif
+
+			/* Spoof the output for valid real files */
+			seq_printf(m, "pos:\t%lli\nflags:\t0%o\nmnt_id:\t%i\nino:\t%lu\n",
+					(long long)file->f_pos, f_flags,
+					susfs_get_non_sus_mnt_id_from_mnt(mnt),
+					path.dentry->d_inode->i_ino);
+			
+			path_put(&path);
+			kfree(pathname);
+			goto bypass_orig_flow;
 		}
 
-		seq_printf(m, "pos:\t%lli\nflags:\t0%o\nmnt_id:\t%i\nino:\t%lu\n",
-				(long long)file->f_pos, f_flags,
-				susfs_get_non_sus_mnt_id_from_mnt(mnt),
-				path.dentry->d_inode->i_ino);
-		path_put(&path);
 		kfree(pathname);
-		goto bypass_orig_flow;
-out_path_put:
-		path_put(&path);
+		goto orig_flow;
+
 out_kfree:
 		kfree(pathname);
 		goto orig_flow;
diff --git a/fs/proc/task_mmu.c b/fs/proc/task_mmu.c
index 179587e52..1f2fbe107 100644
--- a/fs/proc/task_mmu.c
+++ b/fs/proc/task_mmu.c
@@ -371,7 +371,7 @@ show_map_vma(struct seq_file *m, struct vm_area_struct *vma, int is_pid)
 	dev_t dev = 0;
 	const char *name = NULL;
 #ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
-	char *spoofed_redirected_name = kmalloc(PAGE_SIZE, GFP_KERNEL);
+	char *spoofed_redirected_name = kzalloc(PAGE_SIZE, GFP_KERNEL);
 #endif // #ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
 
 	if (file) {
